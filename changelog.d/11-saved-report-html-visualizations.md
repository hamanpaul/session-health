# Saved report HTML visualizations

Add standalone HTML rendering from saved single or batch JSON projections, including legacy batch per-session radars and process-v2 null-preserving evidence charts. The new `scripts/render_saved_report.py` CLI does not re-run parsers, metrics, or model analysis.

