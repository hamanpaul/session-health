# session-health-jev-offline

- [x] Add reproducible regression fixtures and tests for Codex/Copilot pairing,
  bounded portable bundles, seven-axis semantics, and offline CLI status handling.
- [x] Implement the offline parser, portable bundle, process-v2 metrics, and
  terminal/JSON/HTML renderer slice without model, network, or agent CLI calls.
- [x] Preserve ordered typed evidence, identity/provenance checks, recursive
  redaction, import bounds, and full numeric SNR facts across bundle replay.
- [x] Keep raw JSONL input reading bounded but independently configurable, with
  honest partial coverage when a raw input budget is reached.
- [x] Update source documentation, CLI help, changelog, and policy bootstrap for
  the offline behavior and compatibility profile.
- [x] Run the pre-archive OpenSpec, policy, pytest, and synthetic offline
  verification gates for this slice.
